## Redirection to URL after successful auth


  After successfull authentication user will be redirected back to **Application's callback URL** with status flag and token  
```jsx
sample Redirection link
https://<Authorized callback URL>?status=true&token=**<ACCESS TOKEN>**
```