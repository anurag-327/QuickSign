## API Endpoint for token verification
  
**You can use Quick Sign's Developer API's to verify token and ccess details**

```jsx
https://quicksign-bq48.onrender.com/auth/verify
// sample fetch request
const body={
   method:"GET",
   headers:{
      "content-type":"application/json",
      "authorization":"<Client Secret>"
   },
   body:JSON.stringify({token:**<ACCESS TOKEN>**})
}
const response=await fetch("https://quicksign-bq48.onrender.com/api/data/getuser",body);
const data=await response.json();

```


**Response Schema**


```jsx
1- OK Response
{
   staus:200,
   datar:{
      name:<NAME>
      email:<EMAIL>
      type:user,
      profile:<PROFILE IMAGE>
   }
}

2-BAD RESPONSE
{
  status:400|403|404|500,
  message:<ERROR MESSAGE>
}
```