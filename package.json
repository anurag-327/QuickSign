{
  "name": "quicksign",
  "version": "1.0.0",
  "description": "An oauth provider",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "concurrently \"cd api && npm run start\" \"cd client && npm run dev\" \"cd playground && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
